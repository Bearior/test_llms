# Core PoC dependencies (CPU-only, on-premise; no cloud SDKs).
# Pinned-ish lower bounds; the demo's mock path needs only the first block.

# --- always required (mock demo + eval) ---
openpyxl>=3.1
pillow>=10.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
pytest>=7.4

# --- Stage-1 torch backend (optional; sklearn fallback works without it) ---
# Install the CPU wheels explicitly to avoid pulling a CUDA build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# torch>=2.2
# torchvision>=0.17

# --- optional reader backends ---
# PaddleOCR-VL CPU probe (real inference on synthetic Thai text), Apache-2.0:
#   pip install paddleocr paddlepaddle
# Qwen2.5-VL / Qwen3-VL primary reader (GPU host; Apache-2.0; NOT Typhoon):
#   pip install transformers accelerate   # + torch on a CUDA machine
transformers>=4.45   # present for the Qwen wiring; model load is GPU-gated

# --- cloud-LLM pipeline (thaidoc_llm), optional ---
# Mock provider needs nothing. For a real cloud run, pick one:
#   FREE (default): Google Gemini flash-lite — pip install google-genai
#       + set GEMINI_API_KEY (free key: https://aistudio.google.com/apikey)
#   PAID: Anthropic Claude — pip install anthropic + set ANTHROPIC_API_KEY
# google-genai>=1.0
# anthropic>=0.40
